<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<title>Tel company</title>
</head>
<body>
	<form method="post" name="serviceForm">
		Доступные услуги:
		<br>
		<% if(service.available.length !== 0){ %>
			<%for (let current in service.available){%>
 				<input type="checkbox" name="answer" value="<%= service.available[current] %>"> <%= service.available[current] %><br>
  			<%}%>
  			<br><br>
			<button type="submit" id="submit">Заказать услугу</button>
  		<% } else{ %>
  			-нет доступных услуг
  		<% } %>
		<br><br>
		Текущие заявки:
		<br>
		<% if(order){ %>
			<%for (let current in order.services){%>
 				<%= order.services[current] %><br>
  			<%}%>
  		<% } else{ %>
  			-нет заказов
  		<% } %>
	</form>
	<script src="/js/script_potential.js"></script>
</body>
</html>